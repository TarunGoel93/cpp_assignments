105. Create a program to calculate the power of a number using  pass by value.

#include<iostream>
using namespace std;

int power(int base , int exponent){
    int result=1;
    for(int i=0;i<exponent;i++){
        result*=base;
    }
    return result;
};

int main()
{
    int a,b;
    cout<<"Base : ";
    cin>>a;
    cout<<"Exponent : ";
    cin>>b;
    cout<<power(a,b);
}
