103. Write a program to find the factorial of a number using pass by value.

#include<iostream>
using namespace std;

int factorial(int x){
      int fact=1;
    for(int i=x;i>=1;--i){
      
        fact*=i;
         }
          return fact;
   };

int main()
{
    int a;
    cout<<"Enter a number : ";
    cin>>a;
    
    cout<<"Factorial "<<a<<" : "<<factorial(a)<<endl;
}
